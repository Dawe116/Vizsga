<Window x:Class="FoodifyWPF.MenuManagenment.MenuListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FoodifyWPF.MenuManagenment"
        mc:Ignorable="d"
        Title="MenukListája" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Click="LoadMenus_Click" Content="Betöltés" Margin="5" Height="20"/>
            <Button Click="MenuListDelete_Click" Content="Lista törlése" Margin="5" Height="20"/>
        </StackPanel>
        <DataGrid Grid.Row="1" Name="dtgMenu" AutoGenerateColumns="False">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Név" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Leírás" Binding="{Binding Description}"/>
                <DataGridTextColumn Header="Ár" Binding="{Binding Price}"/>
                <DataGridTextColumn Header="ÉtteremId" Binding="{Binding RestaurantId}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
