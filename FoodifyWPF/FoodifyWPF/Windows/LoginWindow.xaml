<Window x:Class="FoodifyWPF.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FoodifyWPF.Windows"
        mc:Ignorable="d"
        Title="Bejelentkezés" Height="300" Width="300">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Margin" Value="5,0,0,5"/>
        </Style>
    </Window.Resources>
    
    <Grid>
        <StackPanel>
            <Label Content="Felhasználónév: "/>
            <TextBox Name="tbxUsername" Text="Dawe"/>

            <Label Content="Jelszó: "/>
            <PasswordBox Name="tbxPassword" Password="cigany"/>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Bejelentkezés" Click="Login_Click"/>
                <Button Content="Mégse" Click="Cancel_Clcik"/>
            </StackPanel>

        </StackPanel>
    </Grid>
</Window>
